<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.projet12joiefull" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- special theme for loading -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Fond orange -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <!-- Ton logo SVG importé -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <!-- Durée de l'icône (optionnel) -->
        <item name="windowSplashScreenAnimationDuration">200</item>
        <!-- APRÈS le splash, on revient au thème par défaut -->
        <item name="postSplashScreenTheme">@style/Theme.projet12joiefull</item>
    </style>
</resources>
